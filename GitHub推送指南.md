# GitHub 仓库推送完整指南

## 🚀 准备工作

### 1. 安装Git（如果尚未安装）
```bash
# Windows: 下载并安装 Git for Windows
# https://git-scm.com/download/win

# 验证安装
git --version
```

### 2. 配置Git用户信息
```bash
git config --global user.name "您的GitHub用户名"
git config --global user.email "您的GitHub邮箱"
```

### 3. 创建GitHub仓库
1. 登录 https://github.com
2. 点击右上角的 "+" → "New repository"
3. 填写仓库信息：
   - Repository name: `ComfyUI_RN_Transtor`
   - Description: `基于大模型API的专业翻译插件，支持OpenAI、DeepSeek等多种API`
   - 选择 Public（公开）或 Private（私有）
   - 不要初始化 README（因为本地已有）
   - 点击 "Create repository"

## 📁 初始化本地Git仓库

### 1. 进入项目目录
```bash
cd D:\Github\ComfyUI_RN_Transtor
```

### 2. 初始化Git仓库
```bash
git init
```

### 3. 添加所有文件到暂存区
```bash
git add .
```

### 4. 提交初始版本
```bash
git commit -m "初始提交：ComfyUI翻译插件，支持多语言翻译和多种API"
```

## 🔗 连接远程仓库

### 方法1：HTTPS方式（推荐新手）
```bash
git remote add origin https://github.com/您的用户名/ComfyUI_RN_Transtor.git
```

### 方法2：SSH方式（需要配置SSH密钥）
```bash
git remote add origin git@github.com:您的用户名/ComfyUI_RN_Transtor.git
```

## ⬆️ 推送到GitHub

### 1. 推送到主分支
```bash
git push -u origin main
```

**注意**：如果GitHub默认分支是 `master` 而本地是 `main`：
```bash
# 重命名本地分支
git branch -m main master
# 然后推送
git push -u origin master
```

### 2. 如果遇到推送错误

#### 错误：远程仓库已存在文件
```bash
# 先拉取远程内容
git pull origin main --allow-unrelated-histories
# 再推送
git push -u origin main
```

#### 错误：认证失败（HTTPS）
- 使用个人访问令牌代替密码
- 或配置Git凭据管理器

## 📝 后续更新流程

### 常规更新步骤
```bash
# 1. 查看状态
git status

# 2. 添加修改的文件
git add .
# 或添加特定文件
git add 文件名

# 3. 提交更改
git commit -m "描述您的更改"

# 4. 推送到GitHub
git push
```

### 查看提交历史
```bash
git log --oneline
```

## 🏷️ 创建版本标签（推荐）

### 创建标签
```bash
# 创建版本标签
git tag -a v1.0.0 -m "第一个稳定版本"

# 推送标签到GitHub
git push origin v1.0.0
```

### 查看标签
```bash
git tag
```

## 🔄 分支管理（高级用法）

### 创建功能分支
```bash
# 创建并切换到新分支
git checkout -b feature/新功能名称

# 在新分支上进行开发...

# 切换回主分支
git checkout main

# 合并分支
git merge feature/新功能名称

# 删除分支
git branch -d feature/新功能名称
```

## 🛠️ 常用Git命令速查

```bash
# 查看状态
git status

# 查看提交历史
git log

# 查看分支
git branch

# 切换分支
git checkout 分支名

# 拉取远程更新
git pull

# 推送到远程
git push

# 查看远程仓库
git remote -v

# 撤销修改（谨慎使用）
git checkout -- 文件名

# 回退到指定版本
git reset --hard 提交ID
```

## ⚠️ 注意事项

1. **不要上传敏感信息**：确保 `.gitignore` 文件配置正确
2. **定期提交**：小步快跑，经常提交
3. **写清楚提交信息**：方便日后查看历史
4. **及时推送**：避免本地代码丢失
5. **备份重要分支**：重要功能开发前创建分支

## 🔍 验证推送成功

1. 访问您的GitHub仓库页面
2. 确认所有文件都已上传
3. 检查提交历史是否正确
4. 验证README文件是否正常显示

## 🆘 问题排查

### 如果推送失败
1. 检查网络连接
2. 验证GitHub凭据
3. 确认仓库地址正确
4. 查看错误信息并搜索解决方案

### 获取帮助
```bash
# 查看Git帮助
git help
git help 命令名
```

## 📚 推荐学习资源

- [Git官方文档](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Pro Git book](https://git-scm.com/book/zh/v2)

---

完成以上步骤后，您的ComfyUI_RN_Transtor插件就成功推送到GitHub了！🎉